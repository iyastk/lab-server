<Window x:Class="ClientLocker.PeerTransferWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LabGuard - Send File to Peer" Height="500" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Border Background="#1e293b" CornerRadius="20" BorderBrush="#334155" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Share File" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Transfer files to other computers in the lab" Foreground="#94a3b8" HorizontalAlignment="Center"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Text="Step 1: Select Destination PC" Foreground="#94a3b8" Margin="0,0,0,10"/>
            <ListBox x:Name="StationsList" Grid.Row="2" Background="#0f172a" Foreground="White" BorderThickness="0" Margin="0,0,0,20">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" BorderBrush="#334155" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" Fill="#22c55e" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding}" FontSize="16"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="3" Margin="0,0,0,20">
                <TextBlock Text="Step 2: Choose File" Foreground="#94a3b8" Margin="0,0,0,10"/>
                <Button Content="Select File..." Click="SelectFile_Click" Background="#334155" Foreground="White" Padding="10"/>
                <TextBlock x:Name="SelectedFileText" Text="No file selected" Foreground="#64748b" Margin="0,5,0,0" FontStyle="Italic"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="Cancel_Click" Background="Transparent" Foreground="#94a3b8" Margin="0,0,20,0" BorderThickness="0"/>
                <Button x:Name="SendButton" Content="Send File" Click="Send_Click" IsEnabled="False" Background="#4f46e5" Foreground="White" Padding="20,10" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
